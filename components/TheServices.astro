---
import { services } from "../index.store";
---

<TheSection name="services">
  <H4 ui="text-gray uppercase">МОИ УСЛУГИ</H4>
  <Col>
    <H3>
      Для дизайна нет границ. <br /> Работаю с проектами из любой точки мира!
    </H3>
    <div class="services__list" data-accordion="services" data-multiple="true">
      {
        services.map((service) => (
          <Accordion title={service.title}>
            <H5>{service.desc}</H5>
            <a class="text-lg font-semibold underline" href={service.link}>
              Читать подробнее
            </a>
          </Accordion>
        ))
      }
    </div>
  </Col>
</TheSection>
